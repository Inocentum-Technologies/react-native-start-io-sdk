///
/// JHybridRNStartIoBannerStateUpdater.cpp
/// This file was generated by nitrogen. DO NOT MODIFY THIS FILE.
/// https://github.com/mrousavy/nitro
/// Copyright © 2025 Marc Rousavy @ Margelo
///

#include "JHybridRNStartIoBannerStateUpdater.hpp"
#include "views/HybridRNStartIoBannerComponent.hpp"
#include <NitroModules/NitroDefines.hpp>

namespace margelo::nitro::rnstartiosdk::views {

using namespace facebook;
using ConcreteStateData = react::ConcreteState<HybridRNStartIoBannerState>;

void JHybridRNStartIoBannerStateUpdater::updateViewProps(jni::alias_ref<jni::JClass> /* class */,
                                           jni::alias_ref<JHybridRNStartIoBannerSpec::javaobject> javaView,
                                           jni::alias_ref<JStateWrapper::javaobject> stateWrapperInterface) {
  JHybridRNStartIoBannerSpec* view = javaView->cthis();

  // Get concrete StateWrapperImpl from passed StateWrapper interface object
  jobject rawStateWrapper = stateWrapperInterface.get();
  if (!stateWrapperInterface->isInstanceOf(react::StateWrapperImpl::javaClassStatic())) {
      throw std::runtime_error("StateWrapper is not a StateWrapperImpl");
  }
  auto stateWrapper = jni::alias_ref<react::StateWrapperImpl::javaobject>{
            static_cast<react::StateWrapperImpl::javaobject>(rawStateWrapper)};

  std::shared_ptr<const react::State> state = stateWrapper->cthis()->getState();
  auto concreteState = std::dynamic_pointer_cast<const ConcreteStateData>(state);
  const HybridRNStartIoBannerState& data = concreteState->getData();
  const std::optional<HybridRNStartIoBannerProps>& maybeProps = data.getProps();
  if (!maybeProps.has_value()) {
    // Props aren't set yet!
    throw std::runtime_error("HybridRNStartIoBannerState's data doesn't contain any props!");
  }
  const HybridRNStartIoBannerProps& props = maybeProps.value();
  if (props.format.isDirty) {
    view->setFormat(props.format.value);
    // TODO: Set isDirty = false
  }
  if (props.adTag.isDirty) {
    view->setAdTag(props.adTag.value);
    // TODO: Set isDirty = false
  }
  if (props.onDisappear.isDirty) {
    view->setOnDisappear(props.onDisappear.value);
    // TODO: Set isDirty = false
  }
  if (props.onLoadError.isDirty) {
    view->setOnLoadError(props.onLoadError.value);
    // TODO: Set isDirty = false
  }
  if (props.onReceiveAd.isDirty) {
    view->setOnReceiveAd(props.onReceiveAd.value);
    // TODO: Set isDirty = false
  }
  if (props.onFailedToReceiveAd.isDirty) {
    view->setOnFailedToReceiveAd(props.onFailedToReceiveAd.value);
    // TODO: Set isDirty = false
  }
  if (props.onImpression.isDirty) {
    view->setOnImpression(props.onImpression.value);
    // TODO: Set isDirty = false
  }
  if (props.onClick.isDirty) {
    view->setOnClick(props.onClick.value);
    // TODO: Set isDirty = false
  }

  // Update hybridRef if it changed
  if (props.hybridRef.isDirty) {
    // hybridRef changed - call it with new this
    const auto& maybeFunc = props.hybridRef.value;
    if (maybeFunc.has_value()) {
      std::shared_ptr<JHybridRNStartIoBannerSpec> shared = javaView->cthis()->shared_cast<JHybridRNStartIoBannerSpec>();
      maybeFunc.value()(shared);
    }
    // TODO: Set isDirty = false
  }
}

} // namespace margelo::nitro::rnstartiosdk::views
