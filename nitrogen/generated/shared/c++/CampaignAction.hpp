///
/// CampaignAction.hpp
/// This file was generated by nitrogen. DO NOT MODIFY THIS FILE.
/// https://github.com/mrousavy/nitro
/// Copyright © Marc Rousavy @ Margelo
///

#pragma once

#if __has_include(<NitroModules/NitroHash.hpp>)
#include <NitroModules/NitroHash.hpp>
#else
#error NitroModules cannot be found! Are you sure you installed NitroModules properly?
#endif
#if __has_include(<NitroModules/JSIConverter.hpp>)
#include <NitroModules/JSIConverter.hpp>
#else
#error NitroModules cannot be found! Are you sure you installed NitroModules properly?
#endif
#if __has_include(<NitroModules/NitroDefines.hpp>)
#include <NitroModules/NitroDefines.hpp>
#else
#error NitroModules cannot be found! Are you sure you installed NitroModules properly?
#endif

namespace margelo::nitro::rnstartiosdk {

  /**
   * An enum which can be represented as a JavaScript union (CampaignAction).
   */
  enum class CampaignAction {
    LAUNCH_APP      SWIFT_NAME(launchApp) = 0,
    OPEN_MARKET      SWIFT_NAME(openMarket) = 1,
  } CLOSED_ENUM;

} // namespace margelo::nitro::rnstartiosdk

namespace margelo::nitro {

  // C++ CampaignAction <> JS CampaignAction (union)
  template <>
  struct JSIConverter<margelo::nitro::rnstartiosdk::CampaignAction> final {
    static inline margelo::nitro::rnstartiosdk::CampaignAction fromJSI(jsi::Runtime& runtime, const jsi::Value& arg) {
      std::string unionValue = JSIConverter<std::string>::fromJSI(runtime, arg);
      switch (hashString(unionValue.c_str(), unionValue.size())) {
        case hashString("LAUNCH_APP"): return margelo::nitro::rnstartiosdk::CampaignAction::LAUNCH_APP;
        case hashString("OPEN_MARKET"): return margelo::nitro::rnstartiosdk::CampaignAction::OPEN_MARKET;
        default: [[unlikely]]
          throw std::invalid_argument("Cannot convert \"" + unionValue + "\" to enum CampaignAction - invalid value!");
      }
    }
    static inline jsi::Value toJSI(jsi::Runtime& runtime, margelo::nitro::rnstartiosdk::CampaignAction arg) {
      switch (arg) {
        case margelo::nitro::rnstartiosdk::CampaignAction::LAUNCH_APP: return JSIConverter<std::string>::toJSI(runtime, "LAUNCH_APP");
        case margelo::nitro::rnstartiosdk::CampaignAction::OPEN_MARKET: return JSIConverter<std::string>::toJSI(runtime, "OPEN_MARKET");
        default: [[unlikely]]
          throw std::invalid_argument("Cannot convert CampaignAction to JS - invalid value: "
                                    + std::to_string(static_cast<int>(arg)) + "!");
      }
    }
    static inline bool canConvert(jsi::Runtime& runtime, const jsi::Value& value) {
      if (!value.isString()) {
        return false;
      }
      std::string unionValue = JSIConverter<std::string>::fromJSI(runtime, value);
      switch (hashString(unionValue.c_str(), unionValue.size())) {
        case hashString("LAUNCH_APP"):
        case hashString("OPEN_MARKET"):
          return true;
        default:
          return false;
      }
    }
  };

} // namespace margelo::nitro
